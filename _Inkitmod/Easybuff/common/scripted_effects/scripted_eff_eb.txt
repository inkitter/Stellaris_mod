# d_immense_farmland_deposit d_immense_mineral_deposit d_immense_energy_deposit


generate_easybuff_tiles = {
    every_tile = { clear_deposits = yes }
    clear_buildings = yes
    
    random_tile = {
        limit = { has_deposit = no num_adjacent_tiles = 2 }
        set_deposit = d_immense_farmland_deposit
        random_neighboring_tile = {
            random_neighboring_tile = {
                limit = { has_building = no has_deposit = no num_adjacent_tiles > 3 }
                set_deposit = d_immense_energy_deposit
            }
        }
    }
    random_tile = {
        limit = { has_deposit = no num_adjacent_tiles = 2 }
        set_deposit = d_immense_farmland_deposit
        random_neighboring_tile = {
            random_neighboring_tile = {
                limit = { has_building = no has_deposit = no num_adjacent_tiles > 3 }
                set_deposit = d_immense_energy_deposit
            }
        }
    }
    random_tile = {
        limit = { has_deposit = no num_adjacent_tiles = 2 }
        set_deposit = d_immense_farmland_deposit
        random_neighboring_tile = {
            random_neighboring_tile = {
                limit = { has_building = no has_deposit = no num_adjacent_tiles > 3 }
                set_deposit = d_immense_mineral_deposit
            }
        }
    }
    random_tile = {
        limit = { has_deposit = no num_adjacent_tiles = 2 }
        set_deposit = d_immense_farmland_deposit
        random_neighboring_tile = {
            random_neighboring_tile = {
                limit = { has_building = no has_deposit = no num_adjacent_tiles > 3 }
                set_deposit = d_immense_mineral_deposit
                random_neighboring_tile = {
                    limit = { has_building = no has_deposit = no num_adjacent_tiles > 3 }
                    random_neighboring_tile = {
                        limit = { has_building = no has_deposit = no num_adjacent_tiles > 3 }
                        set_deposit = d_easybuff_energybase
                        random_neighboring_tile = {
                            limit = { has_building = no has_deposit = no }
                            set_building = "building_power_plant_1"
                            set_deposit = d_immense_energy_deposit
                        }
                        random_neighboring_tile = {
                            limit = { has_building = no has_deposit = no }
                            set_building = "building_mining_network_1"
                            set_deposit = d_immense_mineral_deposit
                        }
                        random_neighboring_tile = {
                            limit = { has_building = no has_deposit = no }
                            set_building = "building_mining_network_1"
                            set_deposit = d_immense_mineral_deposit
                        }
                        random_neighboring_tile = {
                            limit = { has_building = no has_deposit = no }
                            set_building = "building_mining_network_1"
                            set_deposit = d_immense_mineral_deposit
                        }
                    }
                }
            }
        }
    }
    random_tile = {
        limit = { has_deposit = no}
        set_deposit = d_immense_farmland_deposit
    }
    random_tile = {
        limit = { has_deposit = no}
        set_deposit = d_immense_farmland_deposit
    }
    random_tile = {
        limit = { has_deposit = no}
        set_deposit = d_immense_mineral_deposit
    }
    random_tile = {
        limit = { has_deposit = no}
        set_deposit = d_immense_mineral_deposit
    }
    random_tile = {
        limit = { has_deposit = no}
        set_deposit = d_immense_energy_deposit
    }
    random_tile = {
        limit = { has_deposit = no}
        set_deposit = d_immense_energy_deposit
    }
    random_tile = {
        limit = { has_deposit = no}
        set_deposit = d_immense_engineering_deposit
    }
    random_tile = {
        limit = { has_deposit = no}
        set_deposit = d_immense_physics_deposit
    }
    random_tile = {
        limit = { has_deposit = no}
        set_deposit = d_immense_society_deposit
    }
}
# d_immense_engineering_deposit d_immense_physics_deposit d_immense_society_deposit
generate_easybuff_tiles2 = {
    every_tile = {
        clear_deposits = yes
    }
    clear_buildings = yes

    random_tile = {
        limit = { has_deposit = no num_adjacent_tiles = 2 }
        set_deposit = d_easybuff_farmland
        random_neighboring_tile = {
            random_neighboring_tile = {
                limit = { has_building = no has_deposit = no num_adjacent_tiles > 3 }
                set_deposit = d_easybuff_research
            }
        }
    }
    random_tile = {
        limit = { has_deposit = no num_adjacent_tiles = 2 }
        set_deposit = d_easybuff_farmland
        random_neighboring_tile = {
            random_neighboring_tile = {
                limit = { has_building = no has_deposit = no num_adjacent_tiles > 3 }
                set_deposit = d_easybuff_energy
            }
        }
    }
    random_tile = {
        limit = { has_deposit = no num_adjacent_tiles = 2 }
        set_deposit = d_easybuff_farmland
        random_neighboring_tile = {
            random_neighboring_tile = {
                limit = { has_building = no has_deposit = no num_adjacent_tiles > 3 }
                set_deposit = d_easybuff_energy
            }
        }
    }
    random_tile = {
        limit = { has_deposit = no num_adjacent_tiles = 2 }
        set_deposit = d_easybuff_farmland
        random_neighboring_tile = {
            random_neighboring_tile = {
                limit = { has_building = no has_deposit = no num_adjacent_tiles > 3 }
                set_deposit = d_easybuff_sr
                random_neighboring_tile = {
                    limit = { has_building = no has_deposit = no num_adjacent_tiles > 3 }
                    random_neighboring_tile = {
                        limit = { has_building = no has_deposit = no num_adjacent_tiles > 3 }
                        set_deposit = d_easybuff_energy
                        random_neighboring_tile = {
                            limit = { has_building = no has_deposit = no }
                            set_building = "building_power_plant_1"
                            set_deposit = d_easybuff_energy
                        }
                        random_neighboring_tile = {
                            limit = { has_building = no has_deposit = no }
                            set_building = "building_mining_network_1"
                            set_deposit = d_easybuff_minerals
                        }
                        random_neighboring_tile = {
                            limit = { has_building = no has_deposit = no }
                            set_building = "building_mining_network_1"
                            set_deposit = d_easybuff_minerals
                        }
                        random_neighboring_tile = {
                            limit = { has_building = no has_deposit = no }
                            set_building = "building_mining_network_1"
                            set_deposit = d_easybuff_minerals
                        }
                    }
                }
            }
        }
    }
    orbital_deposit_tile = {
        random_list = {
            33 = { set_deposit = d_easybuff_mineralso }
            33 = { set_deposit = d_easybuff_energyo }
            33 = { set_deposit = d_easybuff_researcho }
            1 = { set_deposit = d_easybuff_ssr }
        }
    }
}

generate_easybuff_starres = {
    orbital_deposit_tile = {
        random_list = {
            50 = { set_deposit = d_easybuff_researcho }
            50 = { set_deposit = d_easybuff_ssr }
        }
    }
}

# d_easybuff_sr  d_easybuff_research  d_easybuff_resource  d_easybuff_farmland